<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.jrj.vtmatch.basicmatch.matchcore.dao.SendSmsRecordMapper">

    <select id="countSendNumByDate" resultType="int">
        SELECT count(1) from win_send_sms_record where mobile = #{mobile} and  send_date = #{sendDate}
    </select>

</mapper>
